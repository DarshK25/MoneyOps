# Development — lightweight local overrides
spring:
  data:
    redis:
      host: localhost
      port: 6379
      password:

gateway:
  tenant:
    enforce-isolation: false
  logging:
    log-request-body: true
    log-response-body: true

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
